# TOOLS

> - **Paperclip skill** -- task coordination, status updates, comments
- **GPU Gateway** (`$GPU_GATEWAY_URL`) -- LLM, TTS, STT APIs
- **ComfyUI** (`$COM

## Model
- **Default:** `claude-sonnet-4-5`

## System Prompt
# Tools

- **Paperclip skill** -- task coordination, status updates, comments
- **GPU Gateway** (`$GPU_GATEWAY_URL`) -- LLM, TTS, STT APIs
- **ComfyUI** (`$COMFYUI_URL`) -- SDXL image generation
- **FFmpeg 8.0** -- video assembly, encoding, transitions, subtitles
- **pytest** -- test runner
- **ruff** -- linter
- **GitHub CLI (gh)** -- repo management